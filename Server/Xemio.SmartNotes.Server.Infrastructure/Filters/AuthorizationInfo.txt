Here's some pseudo code to authenticate a request (you need the username, the password, the date of the request (ISO 8601 format) and the content that you want to authenticate).

byte[] salt = UTF8.GetBytes(username); //You might need to pad the username to a length of 8 by appending spaces on it's right. "xemio" -> "xemio   "
byte[] authenticationHash = PBKDF2.GetBytes(password, salt, 100000, 128); //Password, Salt, Iterations, Number of Bytes

byte[] contentBytes = UTF8.GetBytes(Content + Request-Date);
byte[] contentHash = HMACSHA256(contentBytes, authenticationHash);
string contentHashString = Base64(contentHash);

Pass this "contentHashString" in the Authorization request header in the format "Xemio Username:contentHashString".