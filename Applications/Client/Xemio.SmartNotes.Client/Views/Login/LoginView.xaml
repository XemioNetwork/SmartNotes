<UserControl x:Class="Xemio.SmartNotes.Client.Views.Login.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="300"
             Height="300"
             Background="#80808080">
    <Grid Background="Transparent">
        
        <GroupBox Header="Anmeldung" Margin="10" >
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                    <RowDefinition Height="40" />
                </Grid.RowDefinitions>

                <TextBox Grid.Row="1" Margin="5" />
                <PasswordBox Grid.Row="2" Margin="5" />

                <Button Grid.Row="3" Margin="5"  Content="Anmelden"  />
            </Grid>
        </GroupBox>
        
    </Grid>
</UserControl>
