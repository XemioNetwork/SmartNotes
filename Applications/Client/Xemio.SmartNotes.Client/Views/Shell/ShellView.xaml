<UserControl x:Class="Xemio.SmartNotes.Client.Views.Shell.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
             MinWidth="1280"
             MinHeight="720">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- The header view -->
        <ContentControl x:Name="Header" Grid.Row="0" />

        <!-- The current content -->
        <ContentControl x:Name="CurrentContent" Grid.Row="1" />
        
        <!-- The login view -->
        <ContentControl x:Name="Login" Grid.RowSpan="3" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
    </Grid>
</UserControl>
