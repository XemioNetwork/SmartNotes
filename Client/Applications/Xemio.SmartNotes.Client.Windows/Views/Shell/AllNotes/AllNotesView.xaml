<UserControl x:Class="Xemio.SmartNotes.Client.Windows.Views.Shell.AllNotes.AllNotesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             
             d:DesignHeight="300" 
             d:DesignWidth="300"
             
             Background="White">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" MinWidth="160" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" MinWidth="200" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- The background for the items list -->
        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Background="{StaticResource subHeaderBackgroundBrush}" />

        <!-- The buttons above the group tree -->
        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="1" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="1" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="1" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button x:Name="AddGroup" Grid.Column="0" Style="{StaticResource SubHeaderButton}" Foreground="White" FontSize="20" Content="+" />
            <Line Grid.Column="1" Style="{StaticResource DividerLine}" Y1="0" Y2="40" />
        </Grid>

        <!-- The group tree -->
        <TreeView x:Name="Folders" Grid.Row="1" Grid.Column="0" HorizontalContentAlignment="Stretch" BorderThickness="0" BorderBrush="{StaticResource grayColorBrush}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding SubFolders}">
                    <TextBlock Text="{Binding Name}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <!-- The splitter -->
        <GridSplitter Grid.Row="1" Grid.Column="1"  Width="5" ResizeBehavior="PreviousAndNext" BorderBrush="Transparent" />
        
    </Grid>
</UserControl>
